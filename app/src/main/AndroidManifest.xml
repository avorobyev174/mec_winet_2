<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.avorobyev174.mec_winet">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@drawable/logo_mec"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AddressActivityTheme">
        <activity
            android:name=".classes.house.HouseActivity"
            android:theme="@style/AddressActivityTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".classes.section.SectionActivity" />
        <activity android:name=".classes.floor.FloorActivity" />
        <activity android:name=".classes.vestibule.VestibuleActivity" />
        <activity android:name=".classes.winet.WinetActivity" />
        <activity android:name=".WinetInfoActivity" />
    </application>

</manifest>